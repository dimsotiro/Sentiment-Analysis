# Sentiment-Analysis
"A complete ML pipeline for classifying tweet text into categories. The project includes text preprocessing, TF-IDF vectorization, training classic ML models (Naive Bayes, Logistic Regression, Linear SVM), handling class imbalance with SMOTE, and generating evaluation reports with accuracy, F1-score, and confusion matrices."
